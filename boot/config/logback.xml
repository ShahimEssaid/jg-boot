<configuration>


    <property name="logPattern" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n"/>
    <timestamp key="logFileStamp" datePattern="yyyy-MM-dd_HH:mm:ss"/>


    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/jg_${logFileStamp}.log</file>
<!--        <file>logs/jg_%d{yyyy-MM-dd_HH:mm:ss.SSS}.log</file>-->
        <append>true</append>
        <!-- set immediateFlush to false for much higher logging throughput -->
        <immediateFlush>true</immediateFlush>
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>

    <!--    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">-->
    <!--        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">-->
    <!--            <fileNamePattern>logs/jg_.%d{yyyy-MM-dd_HH:mm:ss}.log</fileNamePattern>-->
    <!--        </rollingPolicy>-->
    <!--        <encoder>-->
    <!--            <pattern>${logPattern}</pattern>-->
    <!--        </encoder>-->
    <!--    </appender>-->

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>